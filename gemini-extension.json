{
  "name": "bigquery-data-analytics",
  "version": "0.1.0",
  "description": "Connect and interact with BigQuery data using natural language.",
  "mcpServers": {
    "BigQuery": {
      "command": "${extensionPath}${/}toolbox",
      "args": [
        "--prebuilt",
        "bigquery",
        "--stdio"
      ],
      "env": {
        "BIGQUERY_PROJECT": "${BIGQUERY_PROJECT}"
      }
    }
  },
  "contextFileName": "BIGQUERY.md"
}