{
  "name": "bigquery-data-analytics",
  "version": "0.1.0",
  "description": "Connect and interact with a BigQUery database and data.",
  "mcpServers": {
    "BigQuery": {
      "command": "${extensionPath}${/}toolbox",
      "args": [
        "--prebuilt",
        "bigquery",
        "--stdio"
      ],
      "env": {
        "BIGQUERY_PROJECT": "${BIGQUERY_PROJECT}"
      }
    }
  },
  "contextFileName": "BIGQUERY.md"
}